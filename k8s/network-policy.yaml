---
# Network Policy - Kontrola ruchu sieciowego w namespace crypto-perf
# Implementacja: Deny-all by default, explicit allow dla wymaganych komunikacji

# Default Deny All Ingress (secure by default)
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: crypto-deny-all-ingress
  namespace: crypto-perf
spec:
  podSelector: {}
  policyTypes:
  - Ingress

---
# PostgreSQL - Accept traffic from Controller
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: postgres-allow-from-apps
  namespace: crypto-perf
spec:
  podSelector:
    matchLabels:
      app: postgres
  policyTypes:
  - Ingress
  ingress:
  - from:
    - podSelector:
        matchLabels:
          app: benchmark-controller
    ports:
    - protocol: TCP
      port: 5432

---
# Redis - Accept traffic from Controller and all Crypto Nodes
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: redis-allow-from-apps
  namespace: crypto-perf
spec:
  podSelector:
    matchLabels:
      app: message-broker
  policyTypes:
  - Ingress
  ingress:
  - from:
    - podSelector:
        matchLabels:
          app: benchmark-controller
    - podSelector:
        matchExpressions:
        - key: app
          operator: In
          values:
          - node-py-crypto
          - node-py-pycryptodome
          - node-cpp-openssl
          - node-cpp-cryptopp
    ports:
    - protocol: TCP
      port: 6379

---
# Controller - Accept traffic from Crypto Nodes AND Ingress (NGINX)
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: controller-allow-ingress
  namespace: crypto-perf
spec:
  podSelector:
    matchLabels:
      app: benchmark-controller
  policyTypes:
  - Ingress
  ingress:
  - from:
    - podSelector: {}
    # Zezwolenie na ruch z przestrzeni NGINX Ingress Controller
    - namespaceSelector:
        matchLabels:
          kubernetes.io/metadata.name: ingress-nginx
    ports:
    - protocol: TCP
      port: 8000

---
# Crypto Nodes - Accept traffic from Controller AND Ingress (NGINX)
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: crypto-nodes-allow-ingress
  namespace: crypto-perf
spec:
  podSelector:
    matchExpressions:
    - key: app
      operator: In
      values:
      - node-py-crypto
      - node-py-pycryptodome
      - node-cpp-openssl
      - node-cpp-cryptopp
  policyTypes:
  - Ingress
  ingress:
  - from:
    - podSelector:
        matchLabels:
          app: benchmark-controller
    # Zezwolenie na ruch z przestrzeni NGINX Ingress Controller dla testów k6
    - namespaceSelector:
        matchLabels:
          kubernetes.io/metadata.name: ingress-nginx
    ports:
    - protocol: TCP
      port: 8000